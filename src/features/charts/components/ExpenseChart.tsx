"use client";

/**
 * Placeholder for expense charts (e.g. by category, over time).
 * Integrate recharts or similar when ready.
 */
export function ExpenseChart() {
  return (
    <section className="rounded-lg border border-gray-200 dark:border-gray-800 p-4">
      <h2 className="text-lg font-medium text-foreground">Spending overview</h2>
      <p className="mt-2 text-sm text-gray-500 dark:text-gray-400">
        Charts will render here.
      </p>
    </section>
  );
}
