"use client";

/**
 * Placeholder for expense list UI.
 * Replace with real data and actions (add, edit, delete).
 */
export function ExpenseList() {
  return (
    <section className="rounded-lg border border-gray-200 dark:border-gray-800 p-4">
      <h2 className="text-lg font-medium text-foreground">Recent expenses</h2>
      <p className="mt-2 text-sm text-gray-500 dark:text-gray-400">
        Expense list will appear here.
      </p>
    </section>
  );
}
